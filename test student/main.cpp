#include "classes.h"
#include "classes.cpp"
using namespace std;

int main (){
    /*Year *pheadYear = new Year;
    pheadYear -> pNext = new Year; 
    Year *pcurYear = CreateYear (pheadYear);
    Class *pheadClass = nullptr;
    ImportClasses(pheadClass, pcurYear);
    OutPutStu (pheadClass);
    ImportOldData (pcurYear);
    ImportOldStu ("datayear2.csv", pcurYear -> pHeadClass);
    OutputClasses (pcurYear);
     
    int sem;
    createSemester (pcurYear, sem);
    createCourseCSV (pcurYear, sem);
    PrintCourse (pcurYear, sem);
    Runtest(pcurYear, sem);*/
    TestUpdateData ();
    return 0; 
}
